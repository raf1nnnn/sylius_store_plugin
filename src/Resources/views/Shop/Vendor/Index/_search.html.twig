<div class="ui segment">
    <form method="get" action="{{ path('dotit_sylius_store_plugin_shop_store_index') }}" class="ui loadable form">
        <div class="ui stackable grid" id="searchbar">
            <div class="column" id="searchbarTextField">
                {% for filter in stores.definition.enabledFilters %}
                    {{ sylius_grid_render_filter(stores, filter) }}
                {% endfor %}
            </div>
            <div class="right aligned column" id="searchbarButtons">
                <div class="ui buttons">
                    <button type="submit" class="ui primary icon labeled button" {{ sylius_test_html_attribute('search') }}><i class="search icon"></i> {{ 'sylius.ui.search'|trans }}</button>
                    <a href="{{ path('dotit_sylius_store_plugin_shop_store_index') }}" class="ui negative icon labeled button" {{ sylius_test_html_attribute('clear') }}>
                        <i class="cancel icon"></i> {{ 'sylius.ui.clear'|trans }}
                    </a>
                </div>
            </div>
        </div>
    </form>
</div>
